{
  "name": "ondrip-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ myriadflow/ondrip-subgraph",
    "create-local": "graph create --node http://localhost:8020/ myriadflow/ondrip-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ myriadflow/ondrip-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 myriadflow/ondrip-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
